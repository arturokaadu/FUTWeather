import React from "react";

export default function About(){

    return (
        <div>


        </div>
    )



}
// cuando construimos un componente:
// importar react
// si es funcional return devuelve directamente lo que queremos renderizar
// props ? recibe por parametros about(props)
// 